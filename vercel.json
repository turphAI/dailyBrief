{
  "buildCommand": "rm -rf public && bash build.sh",
  "installCommand": "npm install",
  "outputDirectory": "public",
  "functions": {
    "api/**/*.ts": {
      "runtime": "nodejs18.x"
    }
  }
}
