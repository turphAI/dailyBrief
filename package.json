{
  "name": "daily-brief",
  "version": "1.0.0",
  "description": "Daily Brief - A collection of weekly projects",
  "private": true,
  "scripts": {
    "build:all": "npm run build:index && npm run build:w1 && npm run build:w2 && npm run build:w3 && npm run build:arrange",
    "build:index": "cd index && npm install && npm run build",
    "build:w1": "cd w1-resolution && npm install && cd frontend && npm install && npm run build && cd ../..",
    "build:w2": "cd w2-resolution && cd frontend && npm install && npm run build && cd ../..",
    "build:w3": "cd w3-resolution && cd frontend && npm install && npm run build && cd ../..",
    "build:arrange": "bash scripts/arrange-build.sh",
    "dev:index": "cd index && npm run dev",
    "dev:w1": "cd w1-resolution && vercel dev --yes",
    "dev:w2": "cd w2-resolution/frontend && npm run dev",
    "dev:w3": "cd w3-resolution/frontend && npm run dev"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.2",
    "redis": "^4.6.13",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@vercel/node": "^3.1.0"
  }
}
