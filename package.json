{
  "name": "daily-brief",
  "version": "1.0.0",
  "description": "Daily Brief - A collection of weekly projects",
  "private": true,
  "scripts": {
    "build": "npm run build:landing && npm run build:w1",
    "build:landing": "cd index && npm install && npm run build",
    "build:w1": "cd w1-resolution && npm install && cd frontend && npm install && npm run build",
    "build:dist": "bash scripts/build-dist.sh",
    "dev": "npm run dev:landing",
    "dev:landing": "cd index && npm run dev",
    "dev:w1": "cd w1-resolution && vercel dev --yes"
  },
  "dependencies": {},
  "devDependencies": {
    "@vercel/node": "^3.1.0"
  }
}
